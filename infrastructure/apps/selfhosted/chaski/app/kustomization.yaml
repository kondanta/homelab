---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
components:
  - ../../../../components/no-control-plane-top-level-affinity
resources:
  - secret.sops.yaml
  - ocirepository.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: chaski-config
    files:
      - ./resources/chaski.yaml
generatorOptions:
  disableNameSuffixHash: true
